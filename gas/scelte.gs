// Fantabasket — handler aggiornamento foglio Scelte (pick e diritti)
// Da implementare

function handleScelte(payload) {
  return respond({ ok: true, message: "Scelte handler — da implementare" });
}
